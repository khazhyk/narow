package narow;

public class BSH {

	public static int calc(BoardState bs, Config c) {
		// First, go horizontally, then vertically, then rightdown diag, leftdown diag
		
		// Horizontal check
		for (int x = 0; x < bs.height; x++) {
			int arow = 0;
			int last = 0;
			for (int y = 0; y < bs.width; y++) {
				//bs.board[x][y];
			}
		}
		
		
		return 0;
	}
}
